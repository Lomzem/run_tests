- For `git` commit messages, ALWAYS use **conventional commit messages**.
- In the `git` commit messages, give a bullet point list of high-level
  changes in commit message body
- After you have completed a feature/fix, ensure your code does not
  offend `cargo check` or `cargo clippy`
